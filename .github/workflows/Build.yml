name: Build and push images

on:
  push:
    branches: ["main"]
    paths:
      - "backend/**"
      - "frontend/**"
      - ".github/workflows/**"

jobs:
  build-and-deploy:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v6.0.1

    - name: Login to quay.io
      uses: docker/login-action@v3
      with:
        registry: quay.io
        username: ${{ secrets.GHCR_USERNAME }}
        password: ${{ secrets.GHCR_TOKEN }}

    - name: Build app container
      run: |
        docker build -f backend/Containerfile -t arashrahimi / arashr/guestbook:latest ./backend
        

   
    - name: Build redis container
      run: |
        docker build -f frontend/Containerfile -t arashrahimi / arashr/redis:latest ./frontend
        

    - name: Push containers
      run: |
        docker push quay.io/arashrahimi / arashr/guestbook:latest
        docker push quay.io/rafaelnacho1/redis:latest
          
